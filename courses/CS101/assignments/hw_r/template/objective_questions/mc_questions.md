# 选择题 (Multiple Choice Questions)

**说明**：每题 10 分，共 20 分。请在 `objective_answers/my_answers.json` 中填写答案。

---

## MC1: R 数据类型 (10分)

在 R 中，以下哪个函数用于检查对象的类型？

A. `type()`  
B. `class()`  
C. `typeof()`  
D. `check_type()`

**答案格式**：在 `my_answers.json` 中填写 `"MC1": "B"`

---

## MC2: 向量操作 (10分)

以下哪个说法关于 R 向量操作是**正确**的？

A. R 中向量索引从 0 开始  
B. `c(1,2,3) + c(4,5,6)` 结果是 `c(5,7,9)`  
C. 向量只能包含相同类型的元素  
D. 选项 B 和 C 都正确

**答案格式**：在 `my_answers.json` 中填写 `"MC2": "D"`
